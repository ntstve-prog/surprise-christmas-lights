---
// Hero component for Surprise Christmas Lights
---

<section class="hero">
  <div class="hero-background">
    <!-- Christmas lights string -->
    <div class="christmas-lights" aria-hidden="true">
      <div class="light-bulb red"></div>
      <div class="light-bulb green"></div>
      <div class="light-bulb yellow"></div>
      <div class="light-bulb red"></div>
      <div class="light-bulb green"></div>
      <div class="light-bulb yellow"></div>
      <div class="light-bulb red"></div>
      <div class="light-bulb green"></div>
      <div class="light-bulb yellow"></div>
      <div class="light-bulb red"></div>
    </div>

    <div class="snowflakes" aria-hidden="true">
      <div class="snowflake">❅</div>
      <div class="snowflake">❆</div>
      <div class="snowflake">❄</div>
      <div class="snowflake">❅</div>
      <div class="snowflake">❆</div>
    </div>
  </div>

  <div class="hero-content">
    <div class="hero-text">
      <h1 class="seo-title">Professional Christmas Light Installation in Surprise, AZ</h1>
      <p class="hero-headline">Transform Your Home Into a Winter Wonderland</p>
      <p class="hero-subheadline">
        Professional service, fast installation, stunning results. Let us make your home the talk of the neighborhood this holiday season.
      </p>

      <div class="hero-ctas">
        <a href="#quote" class="cta-primary">
          Get Free Quote
        </a>
        <a href="tel:6022078848" class="cta-secondary">
          <svg class="phone-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.67A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"></path>
          </svg>
          <span class="phone-text">
            <span class="call-now">Call Now</span>
            <span class="phone-number">602-207-8848</span>
          </span>
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 600px;
    display: flex;
    align-items: center;
    overflow: hidden;
    background: linear-gradient(135deg, #0f172a 0%, #020617 100%);
  }

  .hero-background {
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  /* Animated snowflakes - optimized for performance */
  .snowflakes {
    position: absolute;
    inset: 0;
    overflow: hidden;
    pointer-events: none;
  }

  .snowflake {
    position: absolute;
    top: -10%;
    color: rgba(255, 255, 255, 0.7);
    font-size: 1.5rem;
    animation: fall linear infinite;
    will-change: transform;
  }

  .snowflake:nth-child(1) {
    left: 15%;
    animation-duration: 12s;
    animation-delay: 0s;
    font-size: 1.3rem;
  }

  .snowflake:nth-child(2) {
    left: 40%;
    animation-duration: 14s;
    animation-delay: 3s;
    font-size: 1.6rem;
  }

  .snowflake:nth-child(3) {
    left: 65%;
    animation-duration: 11s;
    animation-delay: 1.5s;
    font-size: 1.4rem;
  }

  .snowflake:nth-child(4) {
    left: 80%;
    animation-duration: 13s;
    animation-delay: 4s;
    font-size: 1.5rem;
  }

  .snowflake:nth-child(5) {
    left: 25%;
    animation-duration: 15s;
    animation-delay: 2s;
    font-size: 1.2rem;
  }

  @keyframes fall {
    to {
      transform: translateY(110vh) rotate(360deg);
    }
  }

  /* Christmas Lights - Optimized for performance */
  .christmas-lights {
    position: absolute;
    top: 20px;
    left: 0;
    right: 0;
    height: 40px;
    display: flex;
    justify-content: space-evenly;
    align-items: center;
    pointer-events: none;
    z-index: 10;
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  .light-bulb {
    width: 16px;
    height: 16px;
    border-radius: 50%;
    position: relative;
    animation: twinkle 2s ease-in-out infinite;
    will-change: opacity;
  }

  /* Brand colors for bulbs */
  .light-bulb.red {
    background: radial-gradient(circle at 30% 30%, #FCA5A5, #DC2626);
    box-shadow: 0 0 10px rgba(220, 38, 38, 0.6);
  }

  .light-bulb.green {
    background: radial-gradient(circle at 30% 30%, #86EFAC, #16A34A);
    box-shadow: 0 0 10px rgba(22, 163, 74, 0.6);
    animation-delay: 0.66s;
  }

  .light-bulb.yellow {
    background: radial-gradient(circle at 30% 30%, #FDE68A, #FBBF24);
    box-shadow: 0 0 10px rgba(251, 191, 36, 0.6);
    animation-delay: 1.33s;
  }

  /* Performance-optimized twinkle animation - opacity only */
  @keyframes twinkle {
    0%, 100% {
      opacity: 1;
    }
    50% {
      opacity: 0.3;
    }
  }

  /* Responsive sizing for lights */
  @media (min-width: 768px) {
    .christmas-lights {
      top: 30px;
    }

    .light-bulb {
      width: 20px;
      height: 20px;
    }
  }

  @media (min-width: 1024px) {
    .light-bulb {
      width: 24px;
      height: 24px;
    }
  }

  .hero-content {
    position: relative;
    z-index: 1;
    max-width: 1280px;
    margin: 0 auto;
    padding: 4rem 1.5rem;
    width: 100%;
  }

  .hero-text {
    max-width: 700px;
  }

  .seo-title {
    font-size: 1rem;
    font-weight: 600;
    color: #16A34A;
    margin-bottom: 1rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .hero-headline {
    font-size: 2.5rem;
    font-weight: 800;
    line-height: 1.1;
    color: #fff;
    margin-bottom: 1.5rem;
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.5);
  }

  .hero-subheadline {
    font-size: 1.25rem;
    line-height: 1.6;
    color: #E5E7EB;
    margin-bottom: 2.5rem;
  }

  .hero-ctas {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .cta-primary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: #DC2626;
    color: #fff;
    padding: 1rem 2.5rem;
    border-radius: 0.5rem;
    text-decoration: none;
    font-weight: 700;
    font-size: 1.125rem;
    transition: all 0.2s;
    box-shadow: 0 4px 14px rgba(220, 38, 38, 0.4);
  }

  .cta-primary:hover {
    background: #B91C1C;
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(220, 38, 38, 0.5);
  }

  .cta-secondary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    background: #fff;
    color: #111827;
    padding: 1rem 2rem;
    border-radius: 0.5rem;
    text-decoration: none;
    font-weight: 700;
    font-size: 1.125rem;
    transition: all 0.2s;
    box-shadow: 0 4px 14px rgba(0, 0, 0, 0.2);
  }

  .cta-secondary:hover {
    background: #F3F4F6;
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3);
  }

  .phone-icon {
    width: 24px;
    height: 24px;
    flex-shrink: 0;
  }

  .phone-text {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 0.125rem;
  }

  .call-now {
    font-size: 0.75rem;
    font-weight: 600;
    color: #6B7280;
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .phone-number {
    font-size: 1.25rem;
    font-weight: 700;
    color: #111827;
  }

  /* Tablet */
  @media (min-width: 768px) {
    .hero {
      min-height: 700px;
    }

    .hero-content {
      padding: 6rem 2rem;
    }

    .hero-headline {
      font-size: 3.5rem;
    }

    .hero-subheadline {
      font-size: 1.375rem;
    }

    .hero-ctas {
      flex-direction: row;
      gap: 1.5rem;
    }

    .cta-primary,
    .cta-secondary {
      padding: 1.25rem 2.5rem;
    }
  }

  /* Desktop */
  @media (min-width: 1024px) {
    .hero {
      min-height: 800px;
    }

    .hero-headline {
      font-size: 4.5rem;
    }

    .hero-subheadline {
      font-size: 1.5rem;
    }
  }
</style>
